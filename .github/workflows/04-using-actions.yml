name: 04 - Using Actions

on: push

jobs: 
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout COde
        uses: actions/checkout@v4
      - name: Printing Folders
        run: ls

    #0. Checkout the code from the repository
    #1. Install dep for our react applications
    #2. Executed automated tests
