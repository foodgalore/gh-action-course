name: 05 - 1 - Event Filters and Activity Types

on:
  pull_request:
    types: 
      - opened
      - synchronize
    branches:
      - main

jobs:
  echo:
    runs-on: ubuntu-lateset
    steps:
      - run: echo "Running whever a PR is opened or synchronized and base branch is main"